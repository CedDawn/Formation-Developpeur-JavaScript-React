import React from 'react';
import ProfilePage from '../components/ProfilePage';

/**
 * The page who shows up first when you launch the app
 */
export function Id() {
    return (
        <ProfilePage/>
    );
}